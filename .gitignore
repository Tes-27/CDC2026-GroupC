# ข้อมูลดิบ — มีชื่อจริง + ผูกข้อเสนอแนะกับเจ้าของ ห้ามขึ้น public
CDC2026-GroupC.xlsx

# โปรเจกต์ฟอร์มลงทะเบียนเดิม (คนละส่วน)
files/
files.zip

# ระบบ/เครื่องมือ
.DS_Store
.claude/

# ไฟล์ backup จาก Firestore (มีข้อมูลส่วนตัว ไม่ควรขึ้น public)
backup-firestore-*.json
